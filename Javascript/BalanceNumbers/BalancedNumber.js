function balancedNum(number)
{
    let numAsString  = number.toString();
    
    if(numAsString.length == 1){
    return "Balanced"
    }
    elseif(numAsString.length % 2 == 0){
        let leftSum = numAsString.substr(0, (numAsString.length/2));
        leftSum = 
    }
    
}